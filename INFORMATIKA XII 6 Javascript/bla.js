var jmKelas = 7;
var kelasBerfungsi = 4;
var noKelas = 1;


while (noKelas <= kelasBerfungsi){
    console.log('Speaker di kelas ' + noKelas, 'berfungsi dengan baik')
    noKelas++
}
for (;noKelas <= jmKelas; noKelas++){
    console.log('Speaker di kelas ' + noKelas, 'belum berfungsi dengan baik')
}
